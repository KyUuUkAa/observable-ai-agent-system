# PostgreSQL connection used by Agent Session persistence
AGENT_DATABASE_URL=postgresql+asyncpg://postgres:your_password@localhost:5432/career_agent

# Optional: Hugging Face token for higher model download rate limits
HF_TOKEN=
